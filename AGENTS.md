# ITConnect 바이브코딩 파이프라인 (Vibe Coding Pipeline)

이 프로젝트는 비개발자를 위한 5단계 바이브코딩 교육 파이프라인(`itconnect-ai/planning-plugins`)이 설정된 작업 공간입니다.
사용자가 아이디어를 대화로 구체화하여 기획 문서를 먼저 완성하고, 그 문서에 따라 단계별로 구현하도록 돕습니다.

---

## 1. 명령어 및 단계 매핑

사용자가 아래 명령어나 관련 요청을 입력하면 해당하는 스킬(`skills/<name>/SKILL.md`)의 절차를 최우선으로 준수하여 진행합니다.

| 순서 | 명령어 (슬래시/달러/단축) | 스킬명 | 설명 및 산출물 |
| :--- | :--- | :--- | :--- |
| **1** | `/vibe:01-prd`, `$vibe:01-prd`, `/01-prd` | `01-prd` | 아이디어 구체화 및 PRD 작성 → `docs/prd.md`, `docs/setup.md` |
| **2** | `/vibe:02-design`, `$vibe:02-design`, `/02-design` | `02-design` | 디자인 기준 및 시안 제작·검토 → `docs/design-prompt.md`, `design/` |
| **3** | `/vibe:03-frd`, `$vibe:03-frd`, `/03-frd` | `03-frd` | 기능 분해(CRUD·숨은 기능) 및 정책 결정 → `docs/frd.md` |
| **4** | `/vibe:04-trd`, `$vibe:04-trd`, `/04-trd` | `04-trd` | 기술 스택 및 구조 설계 → `docs/trd.md` |
| **5** | `/vibe:05-plan`, `$vibe:05-plan`, `/05-plan` | `05-plan` | EPIC/STORY 개발 계획 수립 → `docs/plan.md` |
| **반복** | `/vibe:build`, `$vibe:build`, `/build` | `build` | 미완료 STORY 1개 구현 및 체크박스 갱신 |
| **수시** | `/vibe:status`, `$vibe:status`, `/status` | `status` | 현재 단계 진행 상황 및 다음 스킬 안내 |
| **도움말** | `/vibe:help`, `$vibe:help`, `/help` | `help` | 전체 진행 과정 안내 및 시작 가이드 |

---

## 2. 대화 및 진행 핵심 규칙

1. **어조**: 친절하고 정중한 한국어 존댓말을 사용합니다.
2. **질문 제한**: 한 번의 응답에 질문은 **최대 2개**까지만 제시합니다.
3. **선택형 의사결정**:
   - 결정이 필요한 질문은 **A/B/C/D 선택형**으로 제공합니다.
   - 각 선택지마다 아래 형식을 준수합니다:
     - 선택지 내용 요약 (1줄)
     - `고르면:` 이 선택으로 서비스나 결과물이 어떻게 바뀌는지 쉬운 말 1줄
     - `이유:` 30~80자의 핵심 개념 미니 강의
     - `추천:` 추천하는 항목과 그 근거
     - 마지막 선택지는 항상 `기타 (직접 입력 — 마음에 드는 안이 없으면 '다시 추천'이라고 답해도 됩니다)`로 구성
4. **판단 위임 처리**:
   - 학습자가 "네", "아무거나", "모르겠어요" 등으로 답하면 추천안을 즉시 채택하고 문서에 `(추천 수용)`을 명시합니다.
5. **어려운 용어 해설**:
   - 전문 개발/기획 용어가 처음 등장할 때 반드시 일상 비유를 곁들인 한 줄 풀이를 제공합니다 (예: "API(서버와 데이터를 주고받는 통로 — 식당의 주문서)").
6. **산출물 디렉터리 고정**:
   - 문서는 오직 `docs/` 내에만 저장/수정합니다.
   - 시안 이미지는 오직 `design/` 내에만 저장합니다.
   - 문서 작성 중에는 첫머리에 `상태: 작성 중 (다음: ...)`을 유지하고, 최종 승인 시 `상태: 완료`로 변경합니다.
